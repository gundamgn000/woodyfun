{/* ==============================
          小樣式（原本就有的）
      =============================== */}
      <style>{`
        .sidebar-item {
          padding: 10px 4px;
          border-radius: 6px;
          font-size: 16px;
          font-weight: 500;
          color: #333;
          transition: 0.2s;
        }
        .sidebar-item:hover {
          background: #f3f3f3;
        }
        .mobile-item {
          padding: 12px 0;
        }
        @keyframes fadeIn {
          from { opacity: 0; }
          to { opacity: 1; }
        }
        .animate-fadeIn {
          animation: fadeIn .25s ease-out;
        }
      `}</style>